#include <stdio.h>
#include <stdlib.h>

int main(int argc, char const *argv[])
{
    system("ls -a -l ./");

    /* int system(const char *command) 
       把command指定的命令名称或程序名称传给要被命令处理器
       执行的主机环境，并在命令完成后返回 */
    
    return 0;
}
