#include <stdio.h>

int main(int argc, char const *argv[])
{
    int ch = 0;
    int n = 0;
    long m = 0;

    printf(">");
    scanf("%x, %ld\n", &ch, &m);

    printf("%d, %d, %ld\n", n, ch, m);
    
    return 0;
}
